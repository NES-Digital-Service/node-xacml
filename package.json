{
  "repository": "NES-Digital-Service/node-xacml",
  "name": "@nes-digital-service/node-xacml",
  "description": "Provides support for authorization decisions from a XACML Policy Decision Point (PDP) in Node.js apps.",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "scripts": {
    "test": "jest --coverage"
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "xml-js": "^1.6.11"
  },
  "devDependencies": {
    "jest": "^27.0.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "jest": {
    "resetMocks": true,
    "coverageThreshold": {
      "global": {
        "statements": 100,
        "branches": 100,
        "functions": 100,
        "lines": 100
      }
    }
  }
}